<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
  <div class="container-fluid d-flex justify-content-between align-items-center flex-wrap">

    <!-- LEFT: Logo + Links -->
    <div class="d-flex align-items-center">
      <a class="navbar-brand me-3" href="/"><i class="fa-solid fa-hotel"></i> TravaHome</a>
      <div class="d-none d-lg-flex">
        <a class="nav-link custom-dark" href="/listings">Explore</a>
        <a class="nav-link custom-dark" href="/listings/new">Start Hosting</a>
      </div>
    </div>

    <!-- CENTER: Search Bar -->
    <div class="flex-grow-1 d-none d-md-flex justify-content-center my-2">
      <form class="d-flex" action="/listings/search" method="get" style="max-width: 400px; width: 100%;">
        <input class="form-control me-2" type="search" name="q" placeholder="Search with title or location">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>

<!-- RIGHT: Auth Links (Desktop only) -->
    <div class="d-none d-lg-flex align-items-center ms-auto mt-2 mt-lg-0">
      <% if (!currentUser) { %>
        <a href="/signup" class="nav-link custom-dark me-3">Signup</a>
        <a href="/login" class="nav-link custom-dark me-3">Login</a>
      <% } else { %>
        <a href="/logout" class="nav-link custom-dark">Logout</a>
      <% } %>
    </div>

    <!-- TOGGLER BUTTON -->
      <button id="themeToggle" class="btn border-0 bg-transparent">
      <i id="themeIcon" class="bi bi-moon-fill"></i>
    </button>
    <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- COLLAPSIBLE MENU (Mobile only) -->
    <div class="collapse navbar-collapse w-100 mt-2" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link custom-dark d-lg-none" href="/listings" >All Listings</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-dark d-lg-none" href="/listings/new">New Listing</a>
        </li>

        <% if (!currentUser) { %>
          <li class="nav-item">
            <a class="nav-link custom-dark d-lg-none" href="/signup" >Signup</a>
          </li>
          <li class="nav-item">
            <a class="nav-link custom-dark d-lg-none" href="/login" >Login</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link custom-dark d-lg-none" href="/logout" >Logout</a>
          </li>
        <% } %>
      </ul>

      <!-- Mobile Search -->
      <form class="d-md-none px-2 mt-2" action="/listings/search" method="get">
        <input class="form-control mb-2" type="search" name="q" placeholder="Search with title or location">
        <button class="btn w-100" style="background-color: #ff385c;" type="submit">Search</button>
      </form>
    </div>

  </div>
</nav>
